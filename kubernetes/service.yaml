apiVersion: v1
kind: Service
metadata:
  name: alpurl-service
  labels:
    app: alpurl
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  selector:
    app: alpurl
